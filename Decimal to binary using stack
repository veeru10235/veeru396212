#include <stdio.h>

int main() {
    int n, st[64], top = -1;
    printf("Enter decimal number: ");
    scanf("%d", &n);

    if (n == 0) { printf("0\n"); return 0; }

    while (n > 0) { st[++top] = n % 2; n /= 2; }
    while (top >= 0) printf("%d", st[top--]);
    printf("\n");
    return 0;
}
